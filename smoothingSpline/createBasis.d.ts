import { Point } from "../types";
import { Matrix } from "mathjs";
export declare const createBasisCol: (x: number, data: Point[]) => Matrix;
export declare const createBasisMatrix: (data: Point[]) => Matrix;
